# TODO.md

這是人類和人工智慧代理的共享項目 TODO 清單。

規則：

- 僅使用標準 Markdown 複選框：`- [ ]`、`- [x]`。
- 保持任務簡短並以行動為導向。
- 在相關階段新增任務。
- 完成工作後，在同一變更集中更新此文件。
- 如果任務變得過時，將其標記為完成並添加簡短的註釋，而不是
  默默地刪除它。
- 如果任務被阻止，請新增 `Blocked:` 以及原因。

## 目前狀態

- [x] 分析遺留腳本並推斷專案意圖。
- [x] 定義目標分階段重構計畫。
- [x] `.agents/AGENTS.md` 中的文件代理移交規則。
- [x] `.agents/SPEC.md` 中的文件配置規格。
- [x] 將代理導向的文件移至 `.agents/` 下。
- [x] 建立共享代理 TODO 清單。
- [x] 使用 `uv` 完成第 0 階段專案基礎。
- [x] 完成第一個階段 1 運行時骨架切片。
- [x] 為未來的代理人定義標準 Git 工作流程。
- [x] 將套件和 CLI 重新命名為 `bybit_automation` / `bybit-automation`。
- [x] 完成第一階段單一進程基於 REST 的執行時間基礎。
- [x] 完成第 2 階段 SQLite 持久性基礎。
- [x] 定義跨平台最小開發環境和重建步驟。
- [x] 完成第 3 階段安全啟動和協調基礎。
- [x] 完成第 4 階段 Redis 快取和協調基礎。
- [x] 完成第 5 階段 WebSocket 同步基礎。
- [x] 完成第 6 階段配置熱重載基礎。
- [x] 完成第 7 階段運轉穩定性基礎。

## 前期3：開發環境標準化

- [x] 在 `.agents/AGENTS.md` 中記錄支援的最低開發環境。
- [x] 使用 `uv` 記錄全新簽出環境重建流程。
- [x] 記錄項目指令必須透過 `uv run` 運作。
- [x] 驗證目前電腦上重建的環境。

## 第 0 階段：使用 `uv` 進行專案基礎

- [x] 在會話開始時檢查目前的開發環境。
- [x] 確認目前的 git 分支是 `dev` 或使用者批准的工作分支。
- [x] 新增 `pyproject.toml`。
- [x] 新增 `uv.lock`。
- [x] 新增 `src/bybit_automation/` 套件骨架。
- [x] 新增安全的無操作 CLI 入口點。
- [x] 在`.agents/SPEC.md`的基礎上加入`configs/config.template.toml`。
- [x] 新增不需要 Bybit 憑證的基準測試。
- [x] 新增或更新本機設定、SQLite 資料、快取的 `.gitignore` 條目
  和虛擬環境。
- [x] 驗證 `uv sync`。
- [x] 透過 `uv run` 驗證套件匯入或 CLI 啟動。
- [x] 透過 `uv run pytest` 驗證測試指令。

## 第 1 階段：單一進程運行時，基於 REST

- [x] 建立 `App` 或 `BotRuntime` 生命週期。
- [x] 新增統一配置載入。
- [x] 新增統一的日誌記錄設定。
- [x] 擷取 `ExchangeClient` 抽象。
- [x] 擷取純 `StrategyEngine` 決策邏輯。
- [x] 擷取純 `RiskManager` 決策邏輯。
- [x] 新增集中式 `OrderManager`。
- [x] 新增 `PositionManager` 和每個符號運行時狀態。
- [x] 確保策略和風險邏輯共用一個執行時間狀態。
- [x] 驗證所有交易所副作用流經 `OrderManager`。
- [x] 在顯式模式防護後面加入特定的 REST/ccxt 交換客戶端。
- [x] 透過 `OrderManager` 進行有線演示/即時訂單執行。
- [x] 將舊的訂單大小和訂單大小舍入遷移到可測試的幫助程序中。
- [x] 在第 2 階段之前審查第 1 階段，並透過 `OrderManager` 發送策略輸入意圖。
- [x] 為不同的策略/風險節奏增加單一進程循環調度。
- [x] 使用 fakes/mocks 新增對 REST 交換客戶端的測試，而不是即時 Bybit。

## 第 2 階段：SQLite 持久化

- [x] 新增 SQLite 連接/引導模組。
- [x] 啟用 WAL 模式。
- [x] 新增訂單和訂單事件的架構。
- [x] 新增位置快照的架構。
- [x] 新增策略決策和風險事件的架構。
- [x] 新增機器人狀態和設定版本的架構。
- [x] 保留追蹤狀態，例如最高利潤和當前層。
- [x] 使用臨時 SQLite 資料庫新增儲存庫測試。

## 第 3 階段：安全啟動與協調

- [x] 啟動時取得未結訂單。
- [x] 在啟動時取得活動位置。
- [x] 將交換狀態與 SQLite 狀態進行比較。
- [x] 清除已確認平倉部位的過時追蹤狀態。
- [x] 防止新位置繼承過時的尾隨狀態。
- [x] 實作 `SAFE_MODE`。
- [x] 啟動狀態不一致時暫停新條目。
- [x] 加入優雅的關閉行為。
- [x] 新增協調和安全模式轉換的測試。

## 第 4 階段：Redis 快取和協調

- [x] 新增 Redis 快取抽象。
- [x] 快取最新價格和近期K線資料。
- [x] 快取活躍部位和未結帳訂單。
- [x] 增加每個符號的鎖定語意。
- [x] 新增配置重新載入訊號機制。
- [x] 定義和測試 Redis 不可用行為。

## 階段 5：WebSocket 同步

- [x] 新增 WebSocket 流事件類型和流抽象。
- [x] 增加公共市場 WebSocket 攝取。
- [x] 新增私人訂單/部位/執行 WebSocket 攝取。
- [x] 新增心跳監控。
- [x] 增加具有退避功能的重新連接。
- [x] 新增陳舊資料檢測。
- [x] 保留 REST 協調作為權威的糾正路徑。

## 第 6 階段：設定熱重載

- [x] 新增設定模式驗證。
- [x] 新增手動重新載入入口點。
- [x] 在 SQLite 中儲存成功的設定版本。
- [x] 拒絕無效配置而不更改運行時配置。
- [x] 拒絕或要求安全重啟以進行不安全的設定變更。
- [x] 新增有效、無效和不安全重新載入的測試。

## 第 7 階段：運作穩定性

- [x] 新增結構化日誌或明確類型的日誌事件。
- [x] 新增健康檢查。
- [x] 新增通知嚴重性等級。
- [x] 新增重試/退避策略。
- [x] 新增斷路器以應對重複故障。
- [x] 新增空運行驗證流程。
- [x] 當執行時期形狀穩定時新增部署註解或幫助程式。

## 演示驗證/候選版本

- [x] 將分階段測試、發布和架構演進計畫記錄在
  `.agents/TESTING_AND_ARCHITECTURE_PLAN.md`。
- [ ] 使用 `uv` 恢復目前開發環境並執行離線基準。
- [ ] 重新運行並記錄針對目前提交的空運行驗證。
- [ ] 新增 Bybit Demo 唯讀預檢指令，交易副作用為零。
- [ ] 將交易環境選擇與訂單執行權限分開。
- [ ] 新增具有真實讀取和阻止交換寫入的演示影子模式。
- [ ] 新增客戶端訂單 ID 和冪等模糊訂單恢復。
- [ ] 在自動平倉之前驗證 `reduceOnly` 和 Bybit 持倉模式行為。
- [ ] 使用 `reconciliation_interval_sec` 安排定期核對。
- [ ] 將重試/退避和斷路器整合到明確安全的運行時操作。
- [ ] 新增外部操作通知頻道和終止開關。
- [ ] 執行經核准的單代碼示範訂單生命週期測試。
- [ ] 執行重啟、網路、Redis、SQLite、過時資料和重複事件故障演習。
- [ ] 完成 72 小時的演示浸泡測試並記錄候選版本證據。

## 架構演變

- [x] 選擇模組化整體架構作為目前架構並記錄決策。
- [ ] 在拆分進程之前定義穩定的內部命令和事件契約。
- [ ] 在採用非同步服務指令之前新增事務寄件匣/收件匣語意。
- [ ] 在演示版本候選版通過後重新評估三服務拆分。
- [ ] 在分散式部署之前將共用 SQLite 替換為多進程持久性儲存。
- [ ] 在每個未來架構中為每個帳戶保留一個受保護的交換器。
